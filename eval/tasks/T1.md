# T1 — SSM math audit (weight 20 %; rubric `eval/RUBRIC.md`)

**Prompt to the candidate (verbatim):** "Audit the block in `inputs/T1/block.py`
against `inputs/T1/derivation.md` and `inputs/T1/invariants.md`. Report per
claim `✓` / `✗ (why + fix)` / `⚠ fragile (condition)` / `⚠ unverified`, with
the math shown and `file:line` citations. Do not modify the code."

**Inputs (frozen copies; sources are consumer-project assets by placeholder):**
- `inputs/T1/block.py` ← `<consumer-project>/models/<bidirectional-ssm-block-file>.py`
  with *k* planted defects (sealed list: `eval/.sealed/planted-T1.json`)
- `inputs/T1/derivation.md` ← the project's discretization / stability notes
- `inputs/T1/invariants.md` ← the project's pinned invariant table (mask
  convention, fp32 statistics, head divisibility)

**Budget:** read/search only; ≤ 40 tool calls; ≤ 60 min.
**Deliverable:** one verification report (markdown).
**Canary (comparison B):** first line `ACK RULES_HASH=<hash>`.
