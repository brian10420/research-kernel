# T3 — Mamba–Transformer hybrid implementation (weight 20 %)

**Prompt to the candidate (verbatim):** "Implement the variant described in
`inputs/T3/spec.md` in the repository snapshot `inputs/T3/repo-snapshot/`
through the model registry (one file + one registry line). Honor the batch-dict
contract, the mask convention, the central mixed-precision policy, and the
frozen-encoder rules documented in the snapshot. Add the behavioral test the
change needs. State the hand-off to the regression gate. Output complete code
only."

**Inputs:**
- `inputs/T3/repo-snapshot/` ← trimmed copy of
  `<consumer-project>/{models,engine,data,tests}/` with the hidden behavioral
  tests removed (kept sealed for grading)
- `inputs/T3/spec.md` ← the variant specification (a fusion-head or backbone
  option), written so that a correct solution exists within the snapshot

**Budget:** read/search/edit/shell; CPU tests only; ≤ 80 tool calls; ≤ 120 min.
**Deliverable:** the diff + the new test + the hand-off statement.
**Canary (comparison B):** first line `ACK RULES_HASH=<hash>`.
