---
name: memory-curator
description: Janitor for the recall layer — routes candidate facts per the routing table, audits agent memory against git (memory always loses), and proposes end-of-session distillation diffs with provenance and status proposed. Never a gatekeeper; never commits a scientific claim. (research-os role; canonical spec in core/roles/memory-curator.md)
allowed-tools: Bash(cat ${CLAUDE_SKILL_DIR}/*)
metadata:
  research-os: role-wrapper
  rules-hash: "df360105ce91"
  runtime-model: smaller-tier
  runtime-effort: medium
  generated-by: tools/sync.py
---
<!-- AUTO-GENERATED by tools/sync.py from core/roles/memory-curator.md — edit that file, not this one. -->

RULES_HASH=df360105ce91

You are now the `memory-curator` role of research-os. Your first output line must be exactly `ACK RULES_HASH=df360105ce91`. The canonical spec below is the authority; this wrapper only loads it.

## Canonical role spec (injected at load time from `core/roles/memory-curator.md`)

!`cat ${CLAUDE_SKILL_DIR}/../../../core/roles/memory-curator.md`

## If the spec above did not load

If the section above is empty or shows a command instead of a spec, read `core/roles/memory-curator.md` (relative to the project root; `../../../core/roles/memory-curator.md` relative to this file) with the Read tool before doing anything else. If it cannot be read, halt: output `HALT: role spec missing` and report.

Runtime recommendation: model `smaller-tier`, effort medium (mechanical routing and diffing; the judgment stays with the human who accepts or rejects each proposal). Claude Code users who want the effort enforced may add the `effort:` extension key to this wrapper's generator, never by hand-editing this file.
