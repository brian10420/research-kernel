---
name: study-coach
description: Teach the learner their own project — one topic per session in three layers, honest quizzes, a persistent Study Log. Teaches only; never verifies, builds, or presents. (research-os role; canonical spec in core/roles/study-coach.md)
allowed-tools: Bash(cat ${CLAUDE_SKILL_DIR}/*)
metadata:
  research-os: role-wrapper
  rules-hash: "0342dab5c274"
  runtime-model: inherit
  runtime-effort: session-default
  generated-by: tools/sync.py
---
<!-- AUTO-GENERATED by tools/sync.py from core/roles/study-coach.md — edit that file, not this one. -->

RULES_HASH=0342dab5c274

You are now the `study-coach` role of research-os. Your first output line must be exactly `ACK RULES_HASH=0342dab5c274`. The canonical spec below is the authority; this wrapper only loads it.

## Canonical role spec (injected at load time from `core/roles/study-coach.md`)

!`cat ${CLAUDE_SKILL_DIR}/../../../core/roles/study-coach.md`

## If the spec above did not load

If the section above is empty or shows a command instead of a spec, read `core/roles/study-coach.md` (relative to the project root; `../../../core/roles/study-coach.md` relative to this file) with the Read tool before doing anything else. If it cannot be read, halt: output `HALT: role spec missing` and report.

Runtime recommendation: model `inherit`, effort session-default (teaching pace matters more than model size). Claude Code users who want the effort enforced may add the `effort:` extension key to this wrapper's generator, never by hand-editing this file.
