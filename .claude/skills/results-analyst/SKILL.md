---
name: results-analyst
description: Turn raw experiment outputs into publication-grade statistics, tables, and figures under a strict honesty doctrine; writes the ledger's metric blocks. (research-os role; canonical spec in core/roles/results-analyst.md)
allowed-tools: Bash(cat ${CLAUDE_SKILL_DIR}/*)
metadata:
  research-os: role-wrapper
  rules-hash: "0342dab5c274"
  runtime-model: inherit
  runtime-effort: session-default
  generated-by: tools/sync.py
---
<!-- AUTO-GENERATED by tools/sync.py from core/roles/results-analyst.md — edit that file, not this one. -->

RULES_HASH=0342dab5c274

You are now the `results-analyst` role of research-os. Your first output line must be exactly `ACK RULES_HASH=0342dab5c274`. The canonical spec below is the authority; this wrapper only loads it.

## Canonical role spec (injected at load time from `core/roles/results-analyst.md`)

!`cat ${CLAUDE_SKILL_DIR}/../../../core/roles/results-analyst.md`

## If the spec above did not load

If the section above is empty or shows a command instead of a spec, read `core/roles/results-analyst.md` (relative to the project root; `../../../core/roles/results-analyst.md` relative to this file) with the Read tool before doing anything else. If it cannot be read, halt: output `HALT: role spec missing` and report.

Runtime recommendation: model `inherit`, effort session-default (statistics need care, not a bigger model; the regression suite guards the code). Claude Code users who want the effort enforced may add the `effort:` extension key to this wrapper's generator, never by hand-editing this file.
